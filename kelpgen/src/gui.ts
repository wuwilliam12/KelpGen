import GUI from "lil-gui";

// TODO: Using lil-gui for now, switch when have time later
export function createGUI() {
  const gui = new GUI();

  // Species selection

  // Genertion parameters

  // Physics parameters

  return gui;
}